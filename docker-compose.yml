services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: scoreblaze
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5435:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
